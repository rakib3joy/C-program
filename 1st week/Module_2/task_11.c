#include <stdio.h>
int main(){


   int tk;
   scanf("%d", &tk);
   if(tk >= 1000)

      printf("I will eat Pizza");// for the single line, we do not need to give {} brackets.
    // prinf("lp"); //we can use that in Vs code (prettier)
    return 0;
}